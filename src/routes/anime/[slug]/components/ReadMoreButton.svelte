<!-- {#if anim.description && anim.description.split('\n').length > 3}
	<div
		class="h-[10%] w-full bg-gradient-to-t from-surface-600 bottom-0 z-0 flex justify-center items-end"
	>
		<button on:click={() => (isReadMore = !isReadMore)}>Read More</button>
	</div>
{/if} -->
